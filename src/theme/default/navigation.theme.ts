/**
 * This file consists of palette values for theme:default
 */

import { colors } from '../master.theme';
import { Theme } from '@react-navigation/native/lib/typescript/src/types';

const theme: Theme = {
  colors: {
    background: colors.WHITE,
    primary: colors.PORTLAND_ORANGE,
    border: colors.PORTLAND_ORANGE,
    card: colors.MAASTRICHT_BLUE,
    text: colors.WHITE
  },
  dark: false
};

export default theme;
