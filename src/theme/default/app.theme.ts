import { colors } from '../master.theme';

const theme = {
  palette: {
    primaryColor: colors.MAASTRICHT_BLUE,
    secondaryColor: colors.JET,
    primaryAccentColor: colors.PORTLAND_ORANGE,
    secondaryAccentColor: colors.PASTEL_BROWN,
    extrasColor: colors.BLACK_CORAL
  },
  colors: {
    white: colors.WHITE
  }
};

export default theme;
